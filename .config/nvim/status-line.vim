" STATUS LINE ------------------------------------------------------------ {{{

" Lightline configuration.
" Colour scheme.

let g:lightline = {
      \ 'colorscheme': 'dracula',
      \ 'active': {
      \   'left': [ [ 'mode', 'paste' ], [ 'readonly', 'absolutepath', 'modified' ] ],
      \ }
      \ }

" }}}